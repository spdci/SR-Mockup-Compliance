version: '3.3'

services:
  test-app:
    image: sr-api-image
    ports:
      - 3333:3003
    networks:
      - web
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./mockoon-sr.json:/mockoon-sr.json

networks:
  web:
    driver: bridge